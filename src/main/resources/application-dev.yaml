# Local development overrides
logging:
  level:
    root: INFO
    com.nayibit.lifeguide: DEBUG

server:
   port: 8081        # override here if dev needs a different port than the base config

# spring:
#   datasource:
#     url: jdbc:h2:mem:devdb   # add once a DB dependency/datasource exists
